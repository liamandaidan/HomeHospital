import React from "react";
import LandingPage from "../components/LandingPage";

/**
 * @name Home Page
 * @summary This is the landing page that a user will first go to when entering
 * the application. It will render the Landing page component.
 * @author Lance Gee
 * @returns rendered Landing page component to user.
 */
function Home() {
  return <LandingPage />;
}

export default Home;
